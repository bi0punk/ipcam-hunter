camera:
  process_width: 640
  fps_limit: 8
  reconnect_backoff_sec: 2
  detect_every_n_frames: 1

model:
  yolo_weights: "yolov8n.pt"
  conf_thres: 0.35

roi:
  name: "zona_1"
  points: [[409,200],[600,200],[600,292],[409,292]]

logic:
  dwell_seconds: 2.0
  cooldown_seconds: 60
  iou_match_thres: 0.30
  track_ttl_seconds: 2.0

whatsapp:
  session: "default"
  chat_id: "120363047294315024@g.us"
  caption_template: "Alerta: persona en {roi} por {dwell:.1f}s | {ts}"

storage:
  events_dir: "./events"
  save_annotated: true

evidence:
  mode: "isapi"
  timeout_s: 4
  fallback_to_rtsp: true

isapi:
  url: "http://192.168.1.64/ISAPI/Streaming/channels/101/picture"
  auth: "digest"